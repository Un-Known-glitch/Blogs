<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title><%= blog[0].title %></title>
  <link rel="stylesheet" href="/style.css">
  <style>
    body {
      font-family: 'Segoe UI', Tahoma, Verdana, sans-serif;
      background: linear-gradient(to right, #f8f9fa, #e0e0e0);
      margin: 0;
      /* display: flex; */
      justify-content: center;
      align-items: center;
      min-height: 100vh;
    }

    .blog-container {
      max-width: 700px;
      background: #ffffff;
      padding: 30px 40px;
      border-radius: 12px;
      box-shadow: 0 8px 20px rgba(0, 0, 0, 0.1);
    }

    .blog-title {
      font-size: 32px;
      font-weight: 700;
      margin-bottom: 10px;
      color: #222;
    }

    .blog-subtitle {
      font-size: 20px;
      font-weight: 600;
      margin-bottom: 20px;
      color: #555;
    }

    .blog-description {
      font-size: 17px;
      line-height: 1.7;
      color: #444;
      margin-bottom: 30px;
    }

    .btn {
      text-decoration: none;
      background-color: #686868;
      color: white;
      padding: 10px 20px;
      border-radius: 6px;
      transition: background 0.3s;
      margin-right: 10px;
    }

    .btn:hover {
      background-color: #8a8a8a;
    }

    .delete-btn {
      background-color: #d9534f;
    }

    .delete-btn:hover {
      background-color: #c9302c;
    }
  </style>
</head>
<body>

  <%- include("partials/navbar") %>
  <div class="blog-container">
     <% if (blog[0].image) { %>
        <img
         src="/uploads/<%= blog[0].image %>"
         alt="Blog Image"
         style =
         "width: 100%;
         height: auto;
         object-fit:cover;
         border-radius: 5px;
         margin-bottom: 10px;"
         />
         <% } %>
    <div class="blog-title"><%= blog[0].title %></div>
    <div class="blog-subtitle"><%= blog[0].subtitle %></div>
    <div class="blog-description"><%= blog[0].description %></div>

    <a class="btn delete-btn" href="/delete/<%- blog[0].id %>">🗑️ Delete</a>
    <a class="btn delete-btn" href="/edit/<%- blog[0].id %>">✔ Edit</a>
    <a class="btn" href="/">← Back to Blogs</a>
  </div>

</body>
</html>
