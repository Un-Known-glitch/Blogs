<!DOCTYPE html>
<html>
<head>
  <title>Add Blog</title>
  <link rel="stylesheet" href="/style.css">
  <style>
    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background: linear-gradient(to right, #fdfbfb, #ebedee);
      color: #333;
      margin: 0;
      padding: 0;
    }

    .container {
      max-width: 500px;
      margin: 40px auto;
      padding: 30px;
      background: #fff;
      border-radius: 12px;
      box-shadow: 0 8px 24px rgba(0, 0, 0, 0.1);
    }

    h2.head {
      text-align: center;
      font-size: 28px;
      margin-top: 20px;
      color: #222;
    }

    form {
      display: flex;
      flex-direction: column;
    }

    input[type="text"],
    textarea,
    input[type="file"] {
      padding: 12px;
      margin: 10px 0;
      border: 1px solid #ccc;
      border-radius: 6px;
      font-size: 16px;
    }

    textarea {
      resize: vertical;
      min-height: 120px;
    }

    .btn {
      padding: 12px;
      margin-top: 16px;
      background: linear-gradient(to right, #000000, #434343);
      color: white;
      border: none;
      border-radius: 6px;
      font-weight: 600;
      cursor: pointer;
      transition: background 0.3s ease, transform 0.2s;
    }

    .btn:hover {
      background: linear-gradient(to right, #333, #000);
      transform: scale(1.03);
    }

    .link-btn {
      text-align: center;
      margin-top: 10px;
    }

    .link-btn a {
      text-decoration: none;
      color: #ffffff;
      background-color: #007BFF;
      padding: 10px 18px;
      border-radius: 6px;
      display: inline-block;
      font-weight: 600;
      transition: background 0.3s ease;
    }

    .link-btn a:hover {
      background-color: #0056b3;
    }

    .footer {
      text-align: center;
      margin-top: 40px;
      padding: 10px;
      font-size: 14px;
      color: #555;
    }
  </style>
</head>
<body>

  <%- include("partials/navbar") %>

  <h2 class="head">Add a New Blog</h2>

  <div class="container">
    <form action="/add" method="POST" enctype="multipart/form-data">
      <input type="text" name="title" placeholder="Blog Title" required>
      <input type="text" name="subtitle" placeholder="Subtitle" required>
      <textarea name="description" placeholder="Write your blog..." required></textarea>
      <input type="file" name="image" accept="image/*" />
      
      <button type="submit" class="btn">Submit Blog</button>
    </form>

    <div class="link-btn">
      <a href="/">Back to Home</a>
    </div>
  </div>

  <div class="footer">
    Contact Us: test@gmail.com
  </div>

</body>
</html>
